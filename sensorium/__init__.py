"""Sensorium - a multi-agent LLM workflow for passive vision/hearing trend reasoning.

Every node is a pure function ``dict -> dict`` validated against a JSON Schema on both
ends. See ``implementation.md`` for the build order and ``context.md`` for the design.
"""

__version__ = "0.1.0"
